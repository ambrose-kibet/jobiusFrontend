import customFetch from "./axios";
import links from "./links";
import {
  setLocalStorage,
  getLocalStorage,
  removeLocalStorage,
} from "./LocalStorage";
export {
  customFetch,
  links,
  setLocalStorage,
  getLocalStorage,
  removeLocalStorage,
};
